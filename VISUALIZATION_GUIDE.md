# 📸 分析结果图片说明 / Visualization Guide

## 输出图片特性 / Output Image Features

运行分析后，系统会在 `output_images/` 文件夹中生成标注后的图片。每张图片包含以下可视化元素：

After running the analysis, the system generates annotated images in the `output_images/` folder. Each image contains the following visualization elements:

### 1. 人物检测框 / Person Detection Boxes

- **颜色**: 绿色边框 (Green border)
- **标签格式**: `Person #1 (0.95)` 
  - `#1` = 人物编号
  - `0.95` = 检测置信度
- **位置**: 标签显示在检测框上方，带黑色背景以提高可读性

### 2. 横幅/标志检测框 / Banner/Sign Detection Boxes

- **颜色**: 红色边框 (Red border)
- **标签格式**: `Banner #1 (0.92): 检测到的文字内容`
  - `#1` = 横幅编号
  - `0.92` = 检测置信度
  - 显示OCR识别的文字内容（最多50个字符）
- **位置**: 标签显示在检测框下方，带黑色背景

### 3. 图片摘要信息 / Image Summary Legend

在图片左上角显示：
- 图片文件名
- 检测到的人物数量
- 检测到的横幅数量

Displayed in the top-left corner:
- Image filename
- Number of people detected
- Number of banners detected

## 示例输出 / Example Output

```
┌─────────────────────────────────────┐
│ Image: images_1.jpg                 │ ← 摘要信息
│ People: 2                           │
│ Banners: 1                          │
│                                     │
│  ┌─────────┐                        │
│  │ Person  │ ← 绿色框               │
│  │ #1      │                        │
│  └─────────┘                        │
│                                     │
│  ┌─────────┐                        │
│  │ Person  │ ← 绿色框               │
│  │ #2      │                        │
│  └─────────┘                        │
│                                     │
│  ┌─────────────────────┐            │
│  │                     │ ← 红色框   │
│  │  Banner #1:         │            │
│  │  Welcome Text       │ ← OCR文字  │
│  └─────────────────────┘            │
└─────────────────────────────────────┘
```

## 技术特性 / Technical Features

1. **自适应字体大小**: 根据图片尺寸自动调整字体大小
   - Adaptive font size based on image dimensions

2. **高对比度标签**: 所有文字标签都有黑色背景，确保在任何背景下都清晰可见
   - High contrast labels with black backgrounds for visibility

3. **编号系统**: 每个人物和横幅都有唯一编号，便于对照JSON结果
   - Numbering system for easy cross-reference with JSON results

4. **文字截断**: 过长的OCR文字会自动截断并显示省略号
   - Long OCR text is truncated with ellipsis

5. **边界处理**: 标签位置自动调整，避免超出图片边界
   - Smart label positioning to avoid image boundaries

6. **高质量输出**: 保存质量设置为95%，保持图片清晰度
   - High quality output (95% quality) for clarity

## 文件位置 / File Locations

- **输入图片**: `images/` 文件夹
- **输出图片**: `output_images/` 文件夹（自动创建）
- **JSON结果**: `results.json`
- **统计信息**: `statistics.json`

## 查看结果 / Viewing Results

1. **打开输出文件夹**:
   ```cmd
   explorer output_images
   ```

2. **对照JSON文件**: 
   - 打开 `results.json` 查看详细的坐标和置信度
   - 图片中的编号对应JSON中的数组索引

3. **检查统计信息**:
   - 打开 `statistics.json` 查看整体统计

## 自定义可视化 / Customization

如果需要修改可视化样式，可以编辑 `detector.py` 中的 `visualize_detections` 方法：

- 修改颜色: 更改 `person_color` 和 `banner_color`
- 修改字体大小: 调整 `font_size` 计算
- 修改标签格式: 更改 `label` 字符串格式

## 注意事项 / Notes

- 如果图片中没有检测到任何内容，输出图片将只显示摘要信息
- 如果检测框重叠，标签可能会重叠，这是正常现象
- 对于非常大的图片，字体大小会自动缩放以确保可读性

